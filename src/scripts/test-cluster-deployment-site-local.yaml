# Local test DeploymentSite CR (AB#4924: renamed from CommunicationPool, and
# the API version left alpha at the same time).
#
# Only tenantId and deploymentSiteRtId are on the spec; everything the operator
# needs beyond that comes from OperatorOptions (OPERATOR__* env vars).
apiVersion: octo-mesh.meshmakers.io/v1
kind: DeploymentSite
metadata:
  name: meshtest-65d5c447b420da3fb12381a1
  namespace: octo
spec:
  tenantId: "meshtest"
  deploymentSiteRtId: "65d5c447b420da3fb12381a1"
